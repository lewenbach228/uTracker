import { View, Text } from "react-native";
import React from "react";

const SearchTrack = () => {
  return (
    <View style={{ justifyContent: "center", flex: 1, alignItems: "center" }}>
      <Text>rechercher une track precise</Text>
    </View>
  );
};

export default SearchTrack;
